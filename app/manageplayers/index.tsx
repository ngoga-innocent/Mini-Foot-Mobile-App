import { View } from "react-native";
import { Text } from "react-native-paper";

export default function Players() {
  return (
    <View style={{ flex: 1, padding: 20 }}>
      <Text variant="headlineSmall">Players Management (Next Step)</Text>
    </View>
  );
}
